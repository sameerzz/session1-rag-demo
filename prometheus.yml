global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "rag-fastapi"
    static_configs:
      - targets:
          # Prometheus runs locally in Docker, while the API remains deployed on the VM.
          # Scrape the VM's public metrics endpoint rather than localhost.
          - "35.232.150.97:80"
